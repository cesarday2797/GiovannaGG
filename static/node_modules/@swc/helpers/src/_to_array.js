import arrayWithHoles from '@swc/helpers/lib/_array_with_holes';
import iterableToArray from '@swc/helpers/lib/_iterable_to_array';
import nonIterableRest from '@swc/helpers/lib/_non_iterable_rest';

export default function _toArray(arr) {
  return arrayWithHoles(arr) || iterableToArray(arr) || nonIterableRest();
}
