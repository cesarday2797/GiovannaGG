import arrayWithoutHoles from '@swc/helpers/lib/_array_without_holes';
import iterableToArray from '@swc/helpers/lib/_iterable_to_array';
import nonIterableSpread from '@swc/helpers/lib/_non_iterable_spread';

export default function _toConsumableArray(arr) {
  return arrayWithoutHoles(arr) || iterableToArray(arr) || nonIterableSpread();
}
